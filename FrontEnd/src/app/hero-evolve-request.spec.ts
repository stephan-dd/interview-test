import { HeroEvolveRequest } from './hero-evolve-request';

describe('HeroEvolveRequest', () => {
  it('should create an instance', () => {
    expect(new HeroEvolveRequest()).toBeTruthy();
  });
});
